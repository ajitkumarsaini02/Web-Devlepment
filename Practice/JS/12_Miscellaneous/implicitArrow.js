const mul = (a, b) => (
    a * b
);

const sum = (a,b) => a+b; // if there is only one expression, we can skip the curly braces and the return statement

console.log(mul(5, 6));
console.log(sum(19, 4));